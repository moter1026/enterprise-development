name: .NET Tests

on:
  push:
    branches: [ main, labs ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore dependencies
        run: dotnet restore CarRentalPoint/CarRentalPoint.sln

      - name: Build
        run: dotnet build CarRentalPoint/CarRentalPoint.sln --no-restore --configuration Release

      - name: Test
        run: dotnet test CarRentalPoint/CarRentalPoint.sln --no-build --verbosity normal --configuration Release